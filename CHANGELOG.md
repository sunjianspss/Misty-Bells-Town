# Changelog

本文件记录《雾铃小镇》仓库中每一个对外可追踪版本的主要变化。

记录方式参考 Keep a Changelog，版本号采用 Semantic Versioning：

- `MAJOR`：结构性重做或不兼容的内容调整
- `MINOR`：新增一段完整可试玩内容、明显扩展体验范围
- `PATCH`：修复、润色、布局优化、音频与文档类更新

## [Unreleased]

### Added

- 新增 v0.4 阶段 0 视觉规范、素材命名规则、原创方向图与生成提示记录。
- 新增可从标题页直接进入、且不会覆盖正式存档的“桥边黄昏”可玩样片。
- 新增玩家与阿栀四方向四帧透明 PNG 图集，以及风铃、布条、灯笼、水面反光动画图集。
- 新增阶段 1B 高完成度桥边黄昏背景、透明桥栏遮挡层，以及林麦、沈砚、许槐、秦婆婆四方向样片图集。
- 新增阶段 2A 原生 `32 × 32` 草地、泥路、石广场和水面材质图集，每类提供四个稳定变体。
- 新增阶段 2B 面包房 / 木桥、药草棚、剧情道具与河岸四张透明运行时图集，以及对应
  品红底原创源图和可复现处理脚本。
- 新增阶段 2C 分层春树、花簇 / 芦苇、节庆静态附件与春 7 匿名集会人群图集，以及
  四张品红底原创源图和统一处理脚本。
- 新增阶段 2D 八变体草地底纹、跨格不规则草团与 `4 × 4` 低矮地表细节图集，并提供
  可字节级复现的素材处理脚本。
- 新增阶段 2D.1 `v02` 地表细节图集：从既有原创花草图集中重排出更清晰的紫 / 粉 / 金
  碎花、绿草与金色草穗，保持硬 Alpha 和 `32 × 32` 单元契约。
- 新增脚点修正后的阿栀与四位具名村民 `v02` 图集。
- 第五天起开放“桥边共享风痕”面板与留痕弹层，允许玩家把一句短话挂到桥边。
- 新增 `shared-world.js` 与 `shared-world-config.js`，为 Supabase 异步共享桥边痕迹提供可选接入层，并保留本机 fallback。
- 新增桥边共享纸签 / 布条的像素环境演出，以及阿栀、林麦、沈砚对“外乡人痕迹”的轻量环境回应。
- 新增异步共享世界规划文档与 Supabase SQL / RLS 模板。

### Changed

- 原生画布由 `288 × 224` 升级为 `576 × 448`，保留原逻辑网格并以 2× 渲染映射得到 `32 × 32` 图块。
- 桥面、水面、栏杆、桥灯、风铃和黄昏冷暖光照改为分层像素绘制；交互标记移到调色层之后。
- 桥边样片改为背景、水光、人物、桥栏前景、灯火与花瓣分层渲染；两张全幅场景层未同时就绪时原子回退旧画面。
- 阶段 1B 场景将河界、四格桥面及原有固体道具重新锁定到 `18 × 14` 碰撞格，并用桥端挡柱明确可走边界。
- 正式七天流程优先使用阶段 2A 地形图集，并在素材缺失时无缝回退旧的程序绘制。
- 正式七天流程优先使用阶段 2B 分层面包房、四格桥梁、药草棚、公告板、白布桌、
  村口节庆门与模块化河岸；人物可被屋檐、棚面和桥前栏正确遮挡。
- 阶段 2B 剧情道具继续读取原有公告阅读、送货、找种和集会状态，且不改变地图、
  碰撞、互动坐标或存档结构；任一新图集加载失败时退回旧绘制。
- 正式七天流程以阶段 2C 图集替换矩形树、十字花、直线芦苇、桥灯木杆、节庆架、
  春 7 彩条矮架与五个程序人形；地图、碰撞、NPC、剧情、互动和存档格式不变。
- 阿栀和四位具名村民的四方向像素内容统一脚点、比例和硬 Alpha；任务菱形统一移到
  具名村民头顶。
- 正式七天流程的草坪改为三尺度模块化绘制；固定坐标散布会避开道路、水面、固体格、
  NPC 脚点与剧情互动区，新层失败时分别省略或回退阶段 2A / 旧程序草地。
- 阶段 2D.1 将草地散布改为当前日 NPC 清晰区、分带密度和确定性像素错位；高密生态带
  提高碎花 / 草穗数量，但人物脚点、互动格、道路、水面与固体格仍保持清楚。
- 桌面画布固定为 `576px`，320–390px 手机固定为 `288px`，避免任意比例拉伸。
- 渲染循环以带余数校正的节流稳定在 60 FPS，画布离屏时暂停重画，并增加系统“减少动态效果”回退路径。

## [0.3.0] - 2026-04-22

### Added

- 七天完整试玩主线：从初到小镇一路推进到风铃集会当天。
- 5 位核心 / 氛围村民互动，以及村口、广场、面包房、桥边等关键片区对白。
- 公告栏、旧风铃试听、细雨找种、绑布条、试灯、风记签等完整任务链。
- 自动存档、章节导览、试玩总结、重复游玩与底部玩法指引。
- 主背景音乐、环境音、脚步声、对话声、任务提示音与桥边关键铃声。
- 对外试玩封面页、移动端触屏操作与 GitHub Pages 自动部署。
- 基于 `VERSION`、`CHANGELOG.md`、标签工作流的规范发布基础设施。

### Changed

- 桌面端与移动端的游戏区布局分开优化，移动端更强调操作顺序，桌面端更强调整体排版。
- 主 BGM 切换为 `assets/audio/hjm-hjx.mp3`，并调低音量，让环境音和提示音更清楚。
- 发布页文案与试玩入口整理为更适合 GitHub Pages / 对外试玩展示的一版。
