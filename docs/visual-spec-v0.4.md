# 《雾铃小镇》v0.4 视觉规范

状态：阶段 0 定稿 / 阶段 1B 桥边黄昏分层样片完成 / 阶段 2A 地形材质完成 /
阶段 2B 地标、剧情道具与河岸完成 / 阶段 2C 静态核心美术收尾完成 /
阶段 2D.1 花草密度重制完成

## 1. 不可变内容

- 保留 `18 × 14` 地图、全部碰撞格、NPC 坐标、互动 ID、七天剧情、存档格式、键盘与触屏操作。
- 原生画布为 `576 × 448`，玩法仍使用 `288 × 224` 逻辑坐标；渲染层以 `2×` 映射，因此一个逻辑图块对应原生 `32 × 32` 像素。
- 素材必须是原创 PNG；封面只作气氛参考，不裁切或描摹成游戏素材。
- 样片以“春 6 三盏桥灯试亮后的黄昏”为标准状态，不增加地图、角色或剧情。

## 2. 像素与构图

| 项目 | 规格 |
|---|---|
| 地图 | `18 × 14` 逻辑格 |
| 原生画布 | `576 × 448` |
| 环境图块 | `32 × 32` |
| 玩家 / 阿栀 | 可见主体约 `24–30 × 36–40`，精灵单元 `40 × 48` |
| 脚点 | 每格底边中心，必须落在原生整数像素 |
| 桌面显示 | 基础 `576px`；宽屏为原生画布整数 `2×` 的 `1152px` |
| 手机显示 | 主要断点使用 `288px`，即原生画布精确 `1/2` |
| 线条 | 外轮廓 1–2 原生像素；不用纯黑，使用材质深色 |
| 纹理 | 每个材质控制在暗 / 中 / 亮三阶，少量黄昏轮廓光 |

移动插值、阴影、交互框和精灵目标矩形必须先对齐原生像素。禁止连续小数 `bob` 或 `sway`；环境动画使用离散整数帧。

## 3. 主色与材质

| 用途 | 色值 | 说明 |
|---|---|---|
| 深木 | `#56372D` | 桥梁轮廓、榫接、栏杆暗面 |
| 暖木 | `#95603E` | 桥板和建筑受光面 |
| 灯金 | `#F4C467` | 灯芯、铃面、水面最亮反光 |
| 暮桃 | `#DE9471` | 晚霞、阿栀珊瑚衣、暖冷边界 |
| 暮紫 | `#76677E` | 黄昏环境阴影 |
| 深水 | `#355D70` | 河道暗部 |
| 中水 | `#5E8796` | 水面主色 |
| 草影 | `#536A45` | 草丛和岸边暗面 |
| 草地 | `#78925E` | 春日地面 |
| 雾白 | `#E9E2CE` | 薄雾、布条、UI 浅底 |

桥板强调分段木纹、立柱和桥下暗影；水面使用蓝绿底、横向断裂波纹及杏金竖向反光；黄铜铃保留深棕轮廓、赭金中间色与蜜金高光。

## 4. 人物辨识

- 玩家：苔绿圆帽、深青背心、赭黄围巾、棕色斜挎包；不再与沈砚共用草帽轮廓。
- 阿栀：梅紫双髻、珊瑚短衣、蜜金肩带；小个子、轻快、风吹时飘带先动。
- 林麦：发髻、米白围裙、红领巾、面包篮。
- 沈砚：高瘦、窄檐帽、靛蓝长衣、灯钩或铃绳。
- 许槐：宽肩、卷袖、木工围裙和腰锤。
- 秦婆婆：白发髻、微驼背、鼠尾草披肩、药草篮或手杖。

阶段 2C 将阿栀和四位具名村民重新按内容分带切图，统一四方向脚点、比例与硬 Alpha。
完整待机、转身、行走、交谈和专属动作仍留待阶段 3。

## 5. 动画

| 对象 | 帧与节奏 |
|---|---|
| 玩家行走 | 四方向 × 四帧；单格移动完整走完四帧 |
| 玩家 / 阿栀待机 | 两帧，约 `1.4 FPS` |
| 旧风铃 | 四帧，`5–6 FPS` |
| 布条 | 四帧，`4–6 FPS` |
| 水面与灯影 | 四帧，`4 FPS` |
| 灯火 | 熄灭 / 微光 / 点亮 / 呼吸四状态，最高 `6 FPS` |
| 花瓣 | 低密度、离散整数步进 |

启用“减少动态效果”时，环境固定在安全帧；移动、交互和剧情不得依赖动画结束事件。

## 6. 图层顺序

1. 草地底纹、跨格草团、地表碎花与水底
2. 水面反光、桥影与桥板
3. 建筑、桥后栏、风铃架和道具
4. 按脚点排序的人物
5. 桥前栏与前景草叶
6. 雾、雨、花瓣
7. 黄昏冷暖调色
8. 灯光、水面高光
9. 交互框、任务菱形和提示

交互标记必须在调色层之后绘制，复杂场景中仍应一眼可见。

## 7. 命名与图集

单帧规则：

```text
{类别}_{对象}_{动作或状态}_{方向或变体}_f{两位帧号}.png
```

图集规则：

```text
{类别}_{对象}_{动作}_{布局}_{帧数}_v{两位版本号}.png
```

类别使用 `chr`、`tile`、`prop`、`fx`、`ui`。方向使用 `n/e/s/w`；状态使用 `idle/walk/talk/lit/rain/dusk`。

示例：

```text
chr_player_walk_s_f00.png
chr_azhi_idle_e_f01.png
tile_water_dusk_a_f03.png
prop_windchime_old_sway_f03.png
fx_lamp_reflection_water_f02.png
ui_portrait_azhi_64.png
chr_player_walk_4dir_4f_v01.png
```

当前图集契约：

- 玩家：`40 × 48` 单元，行顺序 `n/e/w/s`，列为四步。
- 阿栀：`40 × 48` 单元，行顺序 `s/w/e/n`，列为四步；正式流程使用脚点修正后的 `v02`。
- 四位村民：`40 × 48` 单元，行顺序 `s/w/e/n`，列依次为林麦、沈砚、许槐、秦婆婆；
  正式流程使用统一比例与硬 Alpha 的 `v02`。
- 春 7 匿名人群：`40 × 48` 单元，`5 × 1` 静态图集，列对应既有 `crowd1–5`。
- 阶段 2A 地形：`32 × 32` 单元，行顺序为草地 / 泥路 / 石广场 / 水面，每行四个变体。
- 阶段 2D 草地底纹：`32 × 32` 单元，`4 × 2` 布局；四组冷暖 / 明暗族各有两个
  微纹理变体，正式流程失败时回退阶段 2A 草地。
- 阶段 2D 跨格草团：`64 × 64` 单元，`4 × 2` 布局；每格为硬 Alpha 的不规则
  低对比草团，运行时覆盖既有 `2 × 2` 逻辑格但不改变碰撞。
- 阶段 2D.1 地表细节：正式流程使用 `tile_grass_details_4x4_v02.png`，`32 × 32` 单元、
  `4 × 4` 布局；包含苔影 / 地衣 / 裸土、低矮杂草、紫 / 粉 / 金碎花、金色草穗和
  混合过渡格。花簇原生可见尺寸约 `8–12 × 6–10`，草穗约 `8 × 12`，均为硬 Alpha。
- 阶段 2C 花草：`32 × 32` 单元，`4 × 2` 布局；上行为四种花簇，下行为四种芦苇。
- 阶段 2C 春树：`96 × 96` 单元，`1 × 2` 布局；上行为 base，下行为树冠 foreground。
- 阶段 2C 节庆附件：`64 × 64` 单元，`4 × 2` 布局；上行为广场矮架 / 桥灯挂架，
  下行为四种桥边节庆架状态。
- 阶段 2B 河岸：`32 × 32` 单元，`4 × 2` 布局，包含北岸、西岸、转角与桥口。
- 阶段 2B 地标：`192 × 160` 单元，面包房 / 桥梁各有 base 与 foreground。
- 阶段 2B 药草棚：`96 × 80` 单元，关闭 / 打开种子箱各有 base 与 foreground。
- 阶段 2B 剧情道具：`64 × 64` 单元，`4 × 3` 布局，包含公告板、白布桌与村口门状态。
- 桥边道具：`32 × 32` 单元；第 0–3 行依次为风铃、布条、灯笼、水面反光，列为四帧。
- 阶段 1B 场景：`576 × 448` 无人物背景层与同尺寸透明桥栏前景层，必须成对加载。

## 8. 桥边黄昏验收状态

- 春 6 / 傍晚，春 5 三条布带保留，三盏灯均已点亮。
- 玩家从桥头可继续四方向移动并与沈砚交谈；阿栀保持春 6 原坐标。
- 河道固定为第 `12–17` 列、第 `8–13` 行；桥面固定为第 `11–14` 列、第 `10` 行，右端必须用栏杆、挡柱或岸界明确收口。
- 面包房、公告板、桌台、树与村口标记的可见占地必须覆盖其既有固体格，不得在空地留下隐形碰撞。
- 河面必须同时读出冷蓝绿水色和暖杏金灯影；桥板、风铃、布条和人物轮廓不被黄昏色层吞没。
- 精灵加载失败时自动回退到旧绘制，剧情与操作仍可完成。
- 样片模式不写入或覆盖正式存档。

素材生成来源与最终提示词记录在
[`assets/images/game/v0.4/ART-PROMPTS.md`](../assets/images/game/v0.4/ART-PROMPTS.md)。

## 9. 阶段 2A 当前状态

- 正式七天流程开始使用 `tile_terrain_base_4x4_v01.png` 绘制草地、泥路、石广场与水面。
- 图块变体由地图坐标确定，重载后保持稳定，不改变 `world.path / square / water` 数据。
- 同类四个变体经过色值归一和共享环绕边缘处理，任意组合相邻时外沿像素一致。
- 春 4 雨水、春 6 黄昏和原有水面波纹继续作为运行时图层叠加。
- 图集未就绪或加载失败时自动回退旧的程序绘制，剧情、碰撞和操作保持可用。
- 桥边黄昏样片暂时保留独立整景层；正式七天流程已使用模块化地形、岸线和独立道具。

## 10. 阶段 2B 当前状态

- 正式七天流程使用原创分层 PNG 重制面包房、四格木桥、药草棚、公告板、白布桌、
  村口节庆门与河岸转角。
- 桥梁只覆盖既有第 `11–14` 列可走桥格；左端不再多出一格同质桥板，右端保持原边界。
- 面包房、药草棚和桥梁都有独立前景层；人物经过屋后、棚下或桥上时按脚点形成遮挡关系。
- 公告板响应春 2 未读 / 已读状态；桌台、药草箱和村口门继续响应既有剧情布尔值。
- 桌腿、门柱与公告板支撑收紧到原固体格附近；没有扩大 `world.solids` 或改变互动坐标。
- 四张源图由仓库脚本直接执行色键、硬 Alpha、无抖动减色和图集对齐，可字节级复现。
- 四张新图集同时加载失败时，面包房、桥梁、药草棚、剧情道具和旧岸线均退回原绘制；
  移动、互动、章节跳转与七天剧情仍可完成。
- 春 7 动态布条、纸签、人群动作与连续风铃演出属于阶段 3，不在阶段 2B 烘焙进静态建筑。

## 11. 阶段 2C 当前状态

- 左侧旧矩形树替换为 `48 × 48` 逻辑尺寸的原创分层春树，锚点为 `(-8, 128)`；
  仍只使用原 `(0–1, 9–10)` 四个固体格，右侧树冠只作视觉遮挡，不增加碰撞。
- 十株程序十字花和三丛直线芦苇替换为 `4 × 2` 静态图集，地图坐标与水岸阻挡不变。
- 春 2–7 桥边节庆架、春 6 三张试灯签、春 7 完成愿签与广场三色布卷矮架改用原创 PNG；
  风铃、长布条、灯火、反光、雨线和花瓣仍是独立运行时演出。
- 春 1–5 桥灯保留原灯火状态与光照逻辑，只把矩形木杆替换为缩小后的像素挂架。
- 春 7 已存在的五个匿名集会人形改用五列静态图集；没有新增村民、对白、互动或存档字段。
- 阿栀与四位具名村民使用脚点修正后的 `v02` 图集；各单元只含 `0 / 255` Alpha，
  所有可见脚底落在原生 `y=46`。
- 任务菱形统一放在五位具名村民头顶，继续在调色和灯光之后绘制。
- 六组阶段 2C 图集在同一 `Promise.allSettled` 中分别加载并隔离失败；任一项失败时只回退
  对应旧绘制，不拖累地形、角色或其他道具。
- Playwright 已覆盖春 1、春 4、春 6 黄昏、春 7 清晨 / 黄昏及 `390 × 844` 手机视口；
  原始 `requestAnimationFrame` 采样平均 `8.33 ms`，确认测试浏览器具备高于游戏
  `60 FPS` 节流上限的刷新余量。

## 12. 阶段 2D / 2D.1 当前状态

- 正式七天流程的草地改为三层模块化绘制：中性细草底纹、跨 `2 × 2` 格的不规则明暗
  草团、单格低矮杂草 / 碎花 / 裸土细节；桥边黄昏整景样片保持独立，不重复叠加。
- 底纹的八个变体保持接近的平均亮度，以地图坐标的低频哈希选择；跨格草团只放在四格
  均为草地的位置，避免形成方格边或越过泥路、广场、水岸。
- 地表细节由固定坐标哈希生成，重载、移动和动画期间不会游动；每个细节再作 `±2` 原生
  像素的确定性错位，打散图块中心的行列感。普通草坪每个 `2 × 2` 宏块保留一格纯底纹，
  高密生态带可显示三处强细节，形成接近黄昏样图的花草覆盖率。
- NPC 清晰区只读取当前日角色脚点，面包房门、公告板、白布桌、村口牌、春 4 种子箱和
  桥头互动格继续保持无强细节；不会再用七天 NPC 坐标并集清空远处草坪。任务菱形和
  互动框仍在所有调色层之后绘制。
- 新草地底纹失败时回退 `tile_terrain_base_4x4_v01.png`，再失败则回退旧程序草地；
  跨格草团或地表细节失败时仅省略对应层，不影响剧情、碰撞、存档或操作。
- 三张运行时图集由现有原创 Stage 1B 场景、Stage 2A 地形和 Stage 2C 花草确定性重制；
  2D.1 只把细节层升级到 `v02`，没有裁切封面或烘焙整张地图，相同输入重复运行得到
  字节一致 PNG。
