# 阶段 2D 草地视觉升级 QA

## 验证范围

- 目标：补齐正式七天流程中的草地层次，使其接近阶段 1B 桥边黄昏样片的像素密度与完成度，同时保留现有地图、剧情、碰撞、操作和交互可读性。
- 本轮新增：草地底纹、64 × 64 跨格斑块、32 × 32 微型植被/花朵/裸土细节。
- 本轮不改：18 × 14 地图逻辑、道路、广场、水体、桥梁、人物位置、剧情、存档、移动与交互规则。

## 视觉证据

- 目标参考：`/Users/sun/Documents/gpt-5.4/farm-game/artifacts/stage2d-source-grass-target.png`
  - 尺寸：1280 × 720
  - 状态：阶段 1B 黄昏样片
- 改造前正式流程：`/Users/sun/Documents/gpt-5.4/farm-game/artifacts/stage2d-before-formal-grass.png`
  - 尺寸：3024 × 1776
- 最终实现：`/Users/sun/Documents/gpt-5.4/farm-game/artifacts/stage2d-implementation-desktop.png`
  - 浏览器视口与截图：1440 × 1000
  - 游戏画布显示：1152 × 896，对应 576 × 448 原生画面整数 2 倍缩放
  - 状态：春 1 清晨薄雾正式七天流程
- 合并对照：`/Users/sun/Documents/gpt-5.4/farm-game/artifacts/stage2d-design-qa-comparison.png`
  - 尺寸：1296 × 824
  - 上方为完整场景并排，下方为草地重点区域裁切对照

目标参考与最终实现的时间、天气和地图状态不同，因此本次只比较草地的像素密度、尺度层级、斑块节奏和边缘质量，不比较黄昏/清晨色温，也不要求逐像素复刻场景布局。

## 关键视觉面检查

### 图像与像素质量

- 三张新素材均为原创项目素材的确定性重混，不裁切封面图，也不引入外部素材。
- 底纹保持无缝铺设；斑块使用硬透明边缘跨越 2 × 2 图块；细节保持安全边距，未出现半透明晕边或品红底。
- 最终对照中未发现明显 32 像素方格接缝、忽宽忽窄或棋盘式重复。

### 层级、颜色与密度

- 正式流程草地已形成微观底纹、宏观明暗斑块和小型植被三种尺度。
- 春 1 版本刻意比黄昏样片更冷静、疏朗，仍由运行时天气与时段叠色承担氛围变化。
- 小花与裸土细节保持低对比，不与人物、任务标记和桥灯争夺注意力。

### 布局与交互可读性

- 地图尺寸、道路、建筑、桥梁、水体及 UI 布局未变。
- NPC 脚边、面包房门口、公告牌、摊位、村口牌、种子箱和桥口设置了静态清晰区。
- 任务标记、人物和可交互物仍绘制在草地细节之上，截图中无关键目标被遮挡。
- 字体、文案、图标、按钮、间距与响应式 CSS 均未被本轮改动。

### 降级与稳定性

- `grassBase` 缺失时回退至阶段 2A 地形图块，再回退到原程序化草地。
- `grassPatches` 或 `grassDetails` 单独缺失时只跳过对应层，不阻断游戏加载。
- 所有布点由固定哈希和地图坐标生成，不依赖逐帧随机数，不会产生移动闪烁。

## 迭代记录

1. 第一轮发现 P1：场景裁片形成明显 32 像素方格和高对比重复。改为从阶段 2A 微纹理生成低对比无缝底图。证据：`artifacts/stage2d-qa-iteration-1-grid.jpg`。
2. 第二轮发现 P1：量化细节仍形成棋盘/符号式节奏。降低底图振幅并拆分宏观与微观层。证据：`artifacts/stage2d-qa-iteration-2-checker.jpg`。
3. 第三轮发现 P1：宏观层像独立深色圆岛。缩小明度差，改用不规则硬边遮罩并提高底纹连续性。证据：`artifacts/stage2d-qa-iteration-3-patch-islands.jpg`。
4. 最终轮修复 P2：缩小环境细节、调整素材权重、为每个 2 × 2 区域保留纯底纹格，并加入交互清晰区。最终证据：`artifacts/stage2d-implementation-desktop.png`。

## 功能与技术验证

- `node --check script.js`：通过。
- `node --check assets/game-art-v04.js`：通过。
- `uv run --python 3.12 python -m py_compile scripts/prepare_stage2d_assets.py`：通过。
- 三张图集重新生成后逐字节一致，确定性检查通过。
- 本地服务记录显示三张阶段 2D 素材均返回 HTTP 200。
- 已验证“开始新试玩”“继续上次进度”及正式游戏画面渲染。
- 浏览器在最终桌面画面确认后限制了继续访问本地页面，因此本轮未重新采集手机端、减少动画状态及控制台日志；相关 CSS、动画开关和输入代码未被本轮修改。这是发布前回归测试项，不是当前视觉阻断项。

## 最终发现

- P0：无。
- P1：无。
- P2：无。
- P3：春 1 正式流程的花草密度有意低于黄昏参考；可在阶段 3 环境动画与四季氛围统一时继续微调。

final result: passed
