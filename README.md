# 雾铃小镇

这是一个零依赖的网页试玩 Demo，用来体验《雾铃小镇》第一周 Vertical Slice 的完整节奏：

- 村口遇见阿栀
- 面包房接到“送第一篮点心”的任务
- 广场交付点心
- 傍晚去桥边和沈砚听第一声旧铃
- 第二天查看公告栏，正式得知春季风铃集会
- 第二次送点心，把节日气氛往前推
- 第三天遇见“旧风铃只响半声”，并替许槐试听左、中、右三处风铃声
- 第四天在细雨里帮秦婆婆找回“雨后铃兰”种子
- 第五天帮阿栀把三条春色布条系到桥边，让节日前的忙碌重新抬起来
- 第六天在节日前夜替桥边试亮三盏灯，让“真的快到了”的感觉落下来
- 第七天在风铃集会当天收三句风记签，把这一周等来的第一声真正挂给人听
- 以“今日小记”和试玩总结收束前七天 Demo

## 在线试玩

- GitHub Pages：<https://sunjianspss.github.io/Misty-Bells-Town/>
- 当前仓库已经接入 GitHub Pages 工作流；后续继续向 `main` 分支推送就会自动更新线上版本

## 异步共享风痕

- 现在项目已经接入一层“异步共享痕迹”骨架：从春 5 开始，玩家可以在桥边留下短句风痕
- 默认状态下先以本机模式运行，方便直接试玩与验证 UI
- 一旦把根目录 [`shared-world-config.js`](shared-world-config.js) 填上 Supabase 配置并开启 `enableRemote`，桥边风痕就会切到真正的公共共享模式
- 落地方案见 [`docs/async-shared-world-plan.md`](docs/async-shared-world-plan.md)
- Supabase 表结构与 RLS 模板见 [`docs/supabase-bridge-traces.sql`](docs/supabase-bridge-traces.sql)

## 版本发布

- 当前仓库版本号由根目录 `VERSION` 管理，当前基线为 `0.3.0`
- 更新记录集中写在 [`CHANGELOG.md`](CHANGELOG.md)
- 推送 `main` 会更新在线试玩；推送 `vX.Y.Z` 标签会自动生成 GitHub Release
- 具体发布步骤见 [`docs/releasing.md`](docs/releasing.md)

## v0.4 阶段 0–2D.1：桥边样片与核心静态美术重制

- 已完成一页 [`v0.4 视觉规范与命名规则`](docs/visual-spec-v0.4.md)。
- 标题页新增“体验 v0.4 桥边样片”，会直接进入春 6 三盏桥灯试亮后的黄昏；样片期间暂停章节跳转与跨日，不会覆盖正式存档。
- 也可以直接访问 `?scene=bridge-dusk`；追加 `&motion=reduce` 可冻结环境动画并验证减少动态效果路径。
- 样片保留原 `18 × 14` 地图、碰撞、NPC 坐标、互动 ID、七天剧情和操作方式，只替换渲染层。
- 阶段 1B 已接入 `576 × 448` 高完成度原创场景底图与透明桥栏前景层；人物仍由游戏逻辑实时绘制，不使用烘焙角色。
- 河道、四格桥面、面包房占地、公告板、桌台与村口标记均已对齐原碰撞格；桥右端用可见挡柱收口，不会出现“还有桥板却被隐形墙挡住”。
- 原生画布已升级为 `576 × 448`；玩家与阿栀使用原创透明 PNG 四方向图集，四位村民使用四方向样片图集，水光、灯火、布条和花瓣保持独立动画。
- 场景图层未同时加载完成时会原子回退旧绘制，剧情、移动与互动不会因美术资源失败而中断。
- 阶段 2A 已开始把样片的材质语言推广到正式七天流程：草地、泥路、石广场和水面使用
  原生 `32 × 32` 图块图集与四个稳定变体，加载失败时仍回退原绘制。
- 阶段 2B 已把面包房、四格木桥、春 4 药草棚、公告板、白布桌、村口节庆门和河岸
  转角接入正式七天流程；建筑、棚面与桥前栏使用独立遮挡层。
- 公告板、送货桌、种子箱和村口门继续读取原剧情状态；地图、碰撞格、NPC 坐标、
  互动 ID、存档与七天剧情没有变化。
- 四张阶段 2B 运行时图集可由仓库中的品红底原创源图确定性重建；素材缺失时逐项退回
  旧程序绘制，不影响移动和通关。
- 阶段 2C 已替换旧矩形春树、十字花、直线芦苇、桥灯木杆、节庆架与春 7 彩条矮架，
  并把五个既有匿名集会人形替换为原创静态像素角色；没有新增村民或剧情。
- 阿栀与林麦、沈砚、许槐、秦婆婆使用统一脚点和硬 Alpha 的 `v02` 静态方向图集；
  待机、转身、交谈与专属动作仍留到阶段 3。
- 六张阶段 2C 图集可由 [`scripts/prepare_stage2c_assets.py`](scripts/prepare_stage2c_assets.py)
  从入库源图字节级复现；每项加载失败时只回退对应旧绘制。
- 阶段 2D 已把正式七天流程的草坪补成细草底纹、跨格明暗草团和低矮杂草 / 碎花三层；
  所有散布由地图坐标固定，不会在移动时闪烁或改变。
- 阶段 2D.1 将细节图集升级为 `v02`：紫、粉、金三色碎花与绿 / 金草穗形成可见的生态带，
  并以确定性像素错位打散图块中心的棋盘感；高密区每个 `2 × 2` 宏块最多显示三处强细节。
- 清晰区只按当前日 NPC 脚点与既有互动格计算，人物和任务点周围保持可读，远处草坪不再
  因七天 NPC 坐标并集而被过度清空；三张新图集可由
  [`scripts/prepare_stage2d_assets.py`](scripts/prepare_stage2d_assets.py) 从现有原创素材
  字节级复现，任一层失败都不会影响剧情、碰撞或操作。
- 60 FPS 节流采用时间余数校正，在 90 / 120 / 144Hz 刷新率下不会系统性降为 45 / 48 FPS。
- 生成提示、素材来源和运行时文件清单见
  [`assets/images/game/v0.4/ART-PROMPTS.md`](assets/images/game/v0.4/ART-PROMPTS.md)。

## 七天记录

1. 第一天：初到雾铃小镇，先认识阿栀，再帮林麦把第一篮点心送去广场，傍晚第一次去桥边听风。
2. 第二天：公告栏正式贴出“春季风铃集会”，再送第二篮点心，把节日气氛往前推一步。
3. 第三天：旧风铃只响半声，替许槐试听左、中、右三处声音，让桥边那一句重新完整起来。
4. 第四天：细雨压低了小镇的声响，帮秦婆婆找回“雨后铃兰”种子，让节奏从准备转进人物走心。
5. 第五天：雨后初晴，帮阿栀把三条春色布条系到桥边，让安静之后的忙碌重新抬起来。
6. 第六天：节日前夜，替沈砚试亮三盏桥灯，把“开始忙起来了”推进成“真的快到了”。
7. 第七天：风铃集会当天，替阿栀收三句想让风记住的话，写成风记签挂上桥边，最后听见这一周真正等来的第一声。

## 直接打开

可以直接用浏览器打开 `index.html`。

如果你更习惯本地静态服务，也可以在项目目录运行：

```bash
uv run --python 3.12 python -m http.server 8000
```

然后访问 `http://127.0.0.1:8000`。

## 操作

- `WASD` / 方向键：移动
- `E` / 空格 / 回车：交谈与互动
- 手机上可以使用页面下方的触屏按钮

## Demo 体验补充

- 首次进入会先看到试玩封面页
- 会自动在当前浏览器保存进度
- 支持从“章节导览”回到已解锁天数的清晨开头
- 完成第七天后会出现试玩总结页，可以选择重开或留在第七天继续漫游
- 主页增加了可直接对外展示的封面视觉与正式试玩说明
- 已接入三组真实本地音频资源试听：风铃、细雨、集会黄昏
- 已导出发布主视觉：`assets/images/cover-art.jpg`
- 已生成基础发布音频：`assets/audio/*.wav`
- 游戏内音频会在首次点击开始、移动或交互时自动启用
- 现在游玩过程中会自动播放背景音乐、环境音、脚步声、对话声、任务提示音和桥边铃声
- 背景音乐与环境音现在会使用更柔和的淡入淡出切换
- 当前主 BGM 已切换为你提供的 `assets/audio/hjm-hjx.mp3`
- 主 BGM 音量已调低一档，环境音与提示音保持原逻辑
- 对话打开时仍会轻微压低 BGM 与环境音
- 桌面端与移动端的游戏区布局已分开优化：桌面端更强调整体排版，手机端更强调操作顺序
- 游戏底部新增“玩法指引”，会同步显示当前目标与离你最近的下一步
- 第五天起新增“桥边共享风痕”：可以在桥边留下短句风痕，并在桥边看见最近的共享痕迹演出
- 已保留 `scripts/build_music_variants.py` 与 `docs/bgm-redesign-plan.md` 作为后续再扩展 BGM 的方案储备
- 已整理一版后续可执行的 BGM 改造方案：[`docs/bgm-redesign-plan.md`](docs/bgm-redesign-plan.md)

## 当前已实现

- 七天完整试玩主线：从初到小镇一路推进到风铃集会当天
- 5 位核心 / 氛围村民互动：阿栀、林麦、沈砚、许槐、秦婆婆
- 村口、广场、面包房、桥边等核心片区与连续七天对白
- 公告栏、旧风铃试听、找种子、绑布条、试灯、风记签等完整任务链
- 集会当天围观角色与环境演出
- 自动存档、章节导览、试玩总结与重复游玩支持
- 对外发布页封面、章节导览区、移动端触屏操作与底部玩法指引
- 游戏内主背景音乐、环境音、脚步、对话、任务提示与桥边关键铃声
- 异步共享风痕骨架：桥边风痕面板、留痕弹层、桥边共享纸签演出与 NPC 氛围回应

## 部署说明

- 站点通过 GitHub Pages 发布
- 发布工作流位于 `.github/workflows/deploy-pages.yml`
- 版本发布工作流位于 `.github/workflows/release.yml`
- 当前发布站点所需的静态文件：`index.html`、`styles.css`、`script.js`、`shared-world.js`、`shared-world-config.js` 和 `assets/`

## 配置 Supabase 共享风痕

1. 在 Supabase SQL Editor 里执行 [`docs/supabase-bridge-traces.sql`](docs/supabase-bridge-traces.sql)
2. 打开根目录 [`shared-world-config.js`](shared-world-config.js)
3. 填入：
   - `supabaseUrl`
   - `supabaseAnonKey`
   - 把 `enableRemote` 改成 `true`
4. 推送到 `main` 后，GitHub Pages 上的桥边共享风痕就会切到公共模式

## 后续适合继续做

- 进入阶段 3：补人物待机、转身、交谈与专属动作，并加深围观角色的动作层次
- 继续细化风铃、布带、水面、雨滴和花瓣的环境动画
- 为集会当天补更多即兴对白与轻互动
- 把现在的首轮音频素材继续细化成更完整的环境层、过门、淡入淡出和 UI 声音
- 按 `docs/bgm-redesign-plan.md` 把村庄主题扩成多时间段、多变体结构
- 继续打磨新手引导反馈，比如更明确的章节完成提示与任务完成动效
